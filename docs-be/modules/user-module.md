# Module Người dùng

## Tổng quan

Module chịu trách nhiệm quản lý người dùng, bao gồm quản lý hồ sơ, quyền hạn, và đổi mật khẩu bảo mật.

## API

- `GET /api/v1/users/roles`
- `GET /api/v1/users`
- `GET /api/v1/users/{id}`
- `PUT /api/v1/users/{id}`
- `POST /api/v1/users/change-password`
